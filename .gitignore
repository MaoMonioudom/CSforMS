# Claude Code — personal/local settings, not shared with the team
# (.claude/launch.json stays tracked — that's shared dev-server config)
**/.claude/settings.local.json

# OS junk
.DS_Store
Thumbs.db
